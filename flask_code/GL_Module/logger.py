# import logging
# import time
# filename_t = time.strftime("%Y%m%d-%H%M%S")
# logging.basicConfig(filename = "{}.log".format(filename_t),
#                     level = logging.DEBUG,
#                     format = '%(asctime)s:%(levelname)s:%(name)s:%(message)s')
# logger = logging.getLogger('GL_log')
logger = None
